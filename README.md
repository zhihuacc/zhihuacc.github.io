# How to update
1, Update contents in content folder as you needed.
2, Run `hugo server -D --renderToMemory` to start local server and access it at `http://localhost:1313/`. `--renderToMemory` is to avoid change docs folder.
3, If everything is expected, run `hugo --cleanDestinationDir --minify`
4，Commit and push